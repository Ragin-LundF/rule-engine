rule "rent-payment" {
  description "A recurring payment of at least 500 whose purpose mentions rent or an apartment."
  when
    (purpose contains "rent"
    or purpose contains "apartment")
    and amount >= 500

  then
    label "rent"
}

