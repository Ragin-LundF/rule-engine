rule "established-account" {
  description "Flags an account registered at least 90 days before the review date."
  when
    daysBetween(registeredAt, reviewDate) >= 90
  then
    flag "established"
}
