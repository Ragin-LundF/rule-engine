package ui.workbench.model.mode
/**
 * High-level application area selected in the left icon rail.
 */
enum class AppArea {
    RULES,
    SCHEMA,
    ACTIONS,
    MANIFEST,
    SAMPLES,
    SETTINGS,
}
