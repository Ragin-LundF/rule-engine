package ui.editor.rules

// Lightweight model enums for editor (use a distinct name to avoid collisions
// with platform-specific enums that still exist in JVM sources).
enum class EditorViewMode { CODE, DIAGRAM }


